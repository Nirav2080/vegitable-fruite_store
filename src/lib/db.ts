// This file is intentionally left blank to use in-memory data instead of a real database.
// This is a temporary measure to allow for UI development without a database connection.

const clientPromise = Promise.resolve();
export default clientPromise;
